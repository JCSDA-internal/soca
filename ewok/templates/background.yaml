basename: '{{workdir}}/'
date: '{{background_time}}'

#ice_filename: $(model_ice_file).$(experiment).bg.{{local_current_cycle}}.nc
ice_filename: $(expid).fc.$(fcdatetime).$(fcstep).cice.res.nc
ocn_filename: $(expid).fc.$(fcdatetime).$(fcstep).MOM.res.nc
filename: $(expid).fc.$(fcdatetime).$(fcstep).MOM.res.nc
read_from_file: 1
state variables: [tocn, socn, hocn, ssh] #$(model_variables)

# needed for GetBackground only
# filename: $(current_dir)/$(file_type).$(experiment).bg.{{local_current_cycle}}.nc